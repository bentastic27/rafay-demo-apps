apiVersion: v1
kind: Service
metadata:
  name: bespin-virtualfoundry-service
  namespace: publish-timeout-test
spec:
  ports:
    - port: 8080
      protocol: TCP
  selector:
    app: bespin-virtualfoundry
